function Mostrar()
{
	var peso;
	var contador=0;
	var contenedorMasPesado;
	var contenedorMenosPesado;
	var flag=0;

	for(;;)

	
/*
	while(contador<50)
	{
		peso=parseFloat(prompt("Ingrese el peso del contenedor #"+contador));
		while(peso<=0)
		{
			alert("Ingrese un peso valido");
		}
		if(peso>contenedorMasPesado || flag==0)
		{
			contenedorMasPesado=peso;
		}
		if(peso<contenedorMenosPesado || flag==0)
		{
			contenedorMenosPesado=peso;
			flag=1
		}

		contador++;
	}

	document.write("<br> El contenedor mas pesado es: "+contenedorMasPesado);
	document.write("<br> El contenedorMenosPesado es: "+contenedorMenosPesado);
*/

}

/*
	parseInt sirve para numeros enteros.
	parseFloat sirve para numeros enteros y numeros con coma.
*/